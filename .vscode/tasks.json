{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build project",
            "detail": "Build the project with CMake",
            "group": "build",
            "type": "shell",
            "command": "cmake -S . -B build && cmake --build build",
            "problemMatcher": "$gcc"
        }
    ]
}
