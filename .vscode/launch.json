{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "make -C build && wiiload ./build/wiiu-tower-defense.rpx",
            "options": {
                "env": {
                    "WIILOAD": "tcp:0.0.0.0"
                }
            }
        }
    ]
}
